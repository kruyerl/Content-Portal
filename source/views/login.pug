<!DOCTYPE html>
html(lang="en")
  include ./snippets/head.pug
  body.page-login
    
    section
      h5 Enter your details to continue
      form(action="/action_page.php")
        input(type="input" placeholder="Username" required)
        input(type="input" placeholder="Password" required)
        div
          a(href="#") Forgot Username or Password
          input(type="submit") 
      
    include ./snippets/script.pug
